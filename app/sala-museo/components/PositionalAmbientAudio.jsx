// Copia local de PositionalAmbientAudio
'use client';
import React, { useEffect, useRef, useState, useCallback } from 'react';
import { useThree, useFrame } from '@react-three/fiber';
import { AudioListener, AudioLoader } from 'three';
import { useSound } from '../../../providers/SoundProvider';
import { FRONT_CENTER, BACK_CENTER, HALF_HALL_D } from '../sceneConfig';
export function PositionalAmbientAudio({ active, url='/audio/atmos-museum-quiet-met-nyc-48359.mp3', intensity=1 }){ const { camera }=useThree(); const { muted, soundEnabled, enableAudio }=useSound(); const [listener]=useState(()=>new AudioListener()); const frontAudioRef=useRef(null); const backAudioRef=useRef(null); const bufferRef=useRef(null); const loaderRef=useRef(null); const fadeRef=useRef(0); const targetFadeRef=useRef(0); const frontZ=FRONT_CENTER + HALF_HALL_D - 4; const backZ=BACK_CENTER - HALF_HALL_D + 4; const attachListener=useCallback(()=>{ try { if(!camera.children.includes(listener)) camera.add(listener); }catch{} },[camera, listener]); useEffect(()=>{ if(soundEnabled) return; const unlock=()=>{ enableAudio(); }; window.addEventListener('pointerdown',unlock,{ once:true }); window.addEventListener('keydown',unlock,{ once:true }); return ()=>{ window.removeEventListener('pointerdown',unlock); window.removeEventListener('keydown',unlock); }; },[soundEnabled, enableAudio]); useEffect(()=>{ if(!soundEnabled || muted) return; attachListener(); if(bufferRef.current || loaderRef.current) return; loaderRef.current=new AudioLoader(); loaderRef.current.load(url,(buffer)=>{ bufferRef.current=buffer; [frontAudioRef, backAudioRef].forEach(ref=>{ if(ref.current && !ref.current.buffer){ try{ ref.current.setBuffer(buffer); ref.current.setLoop(true); ref.current.setRefDistance(8); ref.current.setRolloffFactor(1.2); ref.current.setDistanceModel('linear'); ref.current.setVolume(0); ref.current.play(); }catch(e){} } }); targetFadeRef.current=1; }); },[active, soundEnabled, muted, url, attachListener]); useEffect(()=>{ if(!soundEnabled || muted){ targetFadeRef.current=0; return; } targetFadeRef.current= active?1:0; },[active, muted, soundEnabled]); useFrame((_,delta)=>{ const front=frontAudioRef.current; const back=backAudioRef.current; if(!front||!back) return; fadeRef.current += (targetFadeRef.current - fadeRef.current) * Math.min(1, delta*1.5*4); const z=camera.position.z; const t=(z - backZ)/(frontZ - backZ); const tt=Math.min(1,Math.max(0,t)); const smooth= tt*tt*(3 - 2*tt); const frontVol=smooth; const backVol=1 - smooth; const BASE=0.55*intensity*fadeRef.current; try{ front.setVolume(BASE*frontVol); back.setVolume(BASE*backVol); }catch(e){} }); useEffect(()=>()=>{ try{ camera.remove(listener); }catch{} },[camera, listener]); return (<group> <group position={[0,1.8,frontZ]}><positionalAudio ref={n=>frontAudioRef.current=n} args={[listener]} /></group> <group position={[0,1.8,backZ]}><positionalAudio ref={n=>backAudioRef.current=n} args={[listener]} /></group> </group> ); }
